//write a program in c to print all unique elements in an array.

#include<stdio.h>
int main(){
    int i,n,j,k,counter=0;
    printf("input the number of elements stored in the array");
    scanf("%d",&n);
    int arr[n];
    printf(" print all unique element in an array");
    for(i=0;i<n;i++)
    {
        printf("element:%d",i);
        scanf("%d",arr[i]);
    }
    for(j=0;j<n;j++)
    {
        printf("")
    }


}